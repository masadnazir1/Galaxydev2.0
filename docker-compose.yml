services:
  galaxydev-nextjs:
    container_name: galaxydev-nextjs
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3105:3105"
    environment:
      - NODE_ENV=production
    restart: unless-stopped
